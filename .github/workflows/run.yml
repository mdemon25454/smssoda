name: smssoda 24/7 Runner

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-dev libffi-dev libssl-dev

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir python-telegram-bot==13.15 requests beautifulsoup4 phonenumbers APScheduler pytz

      - name: Debug ptb installation
        run: |
          python - << 'EOF'
          import telegram
          print("python-telegram-bot loaded successfully:", telegram.__version__)
          EOF

      - name: Run smssoda.py
        run: |
          python smssoda.py
